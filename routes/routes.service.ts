import { Injectable } from "@nestjs/common";

Injectable()
export class RoutesService {
    tryFunc(){
        let a:Array<Array<number>>;
        b:[]=[1,2,3];
        this.getShortestRoute(a);
    }
    getShortestRoute(allFars:Array<Array<number>>){
        
    }
}

